spring:
  application:
    name: discovery-service
  cloud:
    config:
      uri: http://config-service:9999
eureka:
  server:
    peer-node-read-timeout-ms: 1000
    peer-node-connect-timeout-ms: 500